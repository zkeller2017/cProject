/*
1. Process Text
		- For each line, build string that represents text
2. Choose random piece of text of length: seed
3. Search full text for occurrences- add following lette to a string
	find random leter from the string
	write random letter
	seed = seed.substring(1) + random letter

*/